#include <stdio.h>

int main() {
    char word[] = "hello";
    int length = strlen(word);
    char lastChar = word[length-1];
    
    printf("The last character of the word is: %c\n", lastChar);
    
    return 0;
}
